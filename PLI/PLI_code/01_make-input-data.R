# created 1/7/24
# purpose: make data to input into R package
# notes: using example data from Mette right now

library(tidyverse)
library(readxl)
library(lubridate)

Sys.setenv(LANG = "en")
rm(list = ls())

